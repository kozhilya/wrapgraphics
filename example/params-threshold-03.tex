% !TEX program = lualatex
% !TEX options = --shell-escape
\documentclass{article}
\usepackage{_example_style}

\begin{document}

\section*{Low threshold (0.3)}

More of the image alpha is treated as opaque,
so the contour is closer to the image edge.

\wrapgraphics[threshold=0.3, padding=40, width=0.6\textwidth]{img/tree.png}
\lipsum[1-5]

\end{document}
