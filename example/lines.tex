% !TEX program = lualatex
% !TEX options = --shell-escape
\documentclass{article}
\usepackage{_example_style}

\begin{document}

\section*{Manual line count}

Override the number of wrapped lines with \texttt{lines=5}.
Only the first 5 lines wrap around the image; the rest fall back
to full text width.

\wrapgraphics[width=0.6\textwidth, lines=5]{img/tree.png}
\lipsum[1-10]

\end{document}
