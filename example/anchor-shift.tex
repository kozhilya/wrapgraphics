% !TEX program = lualatex
% !TEX options = --shell-escape
\documentclass{article}
\usepackage{_example_style}

\begin{document}

\section*{Anchor north-west with shift}

The image is anchored to the top-left corner using \texttt{anchor=nw},
with an additional shift: \texttt{shift\_x=30pt, shift\_y=20pt}.

\wrapgraphics[padding=40, scale=0.32, anchor=nw, shift_x=30pt, shift_y=20pt]{img/tree.png}
\lipsum[1-5]

\end{document}
