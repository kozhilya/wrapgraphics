% !TEX program = lualatex
% !TEX options = --shell-escape
\documentclass{article}
\usepackage{_example_style}

\begin{document}

\section*{Explicit image width}

Use \texttt{width=8cm} instead of \texttt{scale} to set image
dimensions directly.

\wrapgraphics[padding=40, width=8cm]{img/tree.png}
\lipsum[1-5]

\end{document}
