Я хочу создать модуль LaTeX, который будет вставлять изображения в документ таким образом, что текст огибает изображение по его альфа-каналу.
Мой основной план:

1. Команда в LaTeX (`\wrapgraphics[args]{image.png}`) вызывает Python-обработчик (аналогично пакету `minted`)
2. В Python на основе изображения `image.png` строится SVG-путь в файл `image.png-wrap-countour.svg`, который будет отстоять на N пикселей (N - аргумент) от альфа-канала изображения (с аргументом трешхолда альфа-канала).
3. На основе этого контура будет строится дополнительная структура в LaTeX, по которой будет происходить огибаение текста (возможно, часть рассчёта будет в Python на прошлом этапе).

Создай базовую структуру для подобного модуля, учти, что для модуля необходимо создать тестовые/демонстрационные файлы, а также документацию.