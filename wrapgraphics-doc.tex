% !TEX program = lualatex
\documentclass{article}

\usepackage{graphicx}
\usepackage{geometry}
\usepackage{xcolor}
\usepackage{hyperref}
\usepackage{enumitem}
\usepackage{caption}
\usepackage{amsmath}
\usepackage{minted}
\usepackage{tabularray}
\usepackage{noto}

\geometry{a4paper, margin=2.5cm}

\hypersetup{colorlinks=true, urlcolor=blue, linkcolor=blue}

\newcommand{\preview}[2][]{%
  \fbox{\includegraphics[#1]{example/#2}}%
}

\newcommand{\previewwidth}{0.3\textwidth}
\newcommand{\pairpreviewwidth}{0.45\textwidth}

\NewDocumentCommand{\exampletable}{m m m O{}}{
  \bigskip
  \noindent
  \begin{tblr}{
    colspec={X[1,l] Q[c,m,wd=\previewwidth]},
    rowspec={Q[m] Q[m] Q[m]},
    cells={blue9},
    cell{1}{1}={blue7,font=\bfseries},
    cell{1}{2}={valign=m},%abovespace=0pt,belowspace=0pt,leftspace=0pt},
    vlines,hlines,
    width=\textwidth,
    % colsep=0pt,
  }
    #1 & \SetCell[r=3]{}\colorbox{white}{\includegraphics[width=\previewwidth]{example/#3}}\\
    #4 \\
    \begin{minipage}{\linewidth}
    \inputminted[breaklines,breakautoindent=false]{latex}{example/_code-#2.tex}
    \end{minipage} \\
  \end{tblr}
}

\NewDocumentCommand{\exampletablepair}{m m m O{}}{
  \bigskip
  \noindent
  \begin{tblr}{
    colspec={X[1,l] Q[c,m,wd=\pairpreviewwidth]},
    rowspec={Q[m] Q[m] Q[m]},
    cells={blue9},
    cell{1}{1}={blue7,font=\bfseries},
    cell{1}{2}={valign=m},
    vlines,hlines,
    width=\textwidth,
  }
    #1 & \SetCell[r=3]{}\colorbox{white}{%
      \begin{minipage}{\pairpreviewwidth}
        \centering
        \includegraphics[width=0.2\textwidth]{example/#3}%
        \hfill
        \includegraphics[page=2,width=0.2\textwidth]{example/#3}%
      \end{minipage}
    }\\
    #4 \\
    \begin{minipage}{\linewidth}
    \inputminted[breaklines,breakautoindent=false]{latex}{example/_code-#2.tex}
    \end{minipage} \\
  \end{tblr}
}

\title{The \texttt{wrapgraphics} package \\[0.5em]
  \large Wrap text around an image's alpha contour}
\author{Ilya I. Kozhemyakin \\ \href{https://github.com/kozhilya/wrapgraphics}{github.com/kozhilya/wrapgraphics}}
\date{Version 1.0 (\today)}

\begin{document}

\maketitle

\begin{abstract}
  \texttt{wrapgraphics} is a Lua\LaTeX{} package that wraps text around an
  image following the contour of its alpha channel. A Python backend
  (Pillow) traces the contour; Lua computes \mintinline{latex}|\parshape| entries.
\end{abstract}

\begin{figure*}[h]
  \centering
  \preview[page=1,width=0.6\textwidth]{basic.pdf}
\end{figure*}

\newpage

\tableofcontents

\newpage

\section{Introduction}

\texttt{wrapgraphics} is a Lua\LaTeX{} package that wraps text around an
image following the contour of its alpha channel.

\begin{minted}{latex}
\usepackage{wrapgraphics}
...
\wrapgraphics[threshold=0.5, padding=5, scale=0.20]{image.png}
\end{minted}

The text following \texttt{\string\wrapgraphics} will flow around the image.
The image is placed as an overlay so it sits inside the paragraph without
breaking the text flow.

The approach used in this package is similar to the approach in the
\href{https://ctan.org/pkg/minted}{\texttt{minted}} package:

\subsection{How it works}

\begin{enumerate}
  \item \mintinline{latex}|\wrapgraphics| saves the image in a box, then
        calls Lua via \texttt{\string\directlua}.
  \item Lua determines the contour SVG path and calls
        \texttt{wrapgraphics.py} if a cached SVG
        with matching parameters does not already exist.
  \item Python extracts the alpha channel, applies threshold and
        dilation (padding), traces the outer contour with the
        Moore--Neighbor boundary following algorithm, and writes a
        \texttt{-shape.svg} file.
  \item Lua parses the SVG, converts pixel coordinates to
        (\texttt{pt} / \texttt{sp}), and builds a flat list of
        \mintinline{latex}|\parshape| entries (indent + width per line).
  \item The \mintinline{latex}|\parshape| is injected into the TeX stream,
        and the image is placed as an overlay (\mintinline{latex}|\rlap|)
        so it sits inside the reflowed paragraph without consuming space.
  \item A \texttt{post\_linebreak\_filter} callback tracks how many
        lines have been consumed and clears the shape when the image height
        is covered, ensuring correct behaviour across page breaks.
\end{enumerate}


\subsection{Special note about AI}
AI agents (namely, Deepseek V4 Flash Free via OpenCode) were used
to create this package, but later its codebase and examples were manually
checked by the author so that the package can be maintained without extra tools.

The author understands that the approach of this package may be sub-optimal: the
contour could probably be calculated with Lua, or the
\mintinline{latex}|\parshape| could be placed with expl3 or with Lua.

But while the author can maintain Python and some Lua produced by AI, he cannot
do the same for complex Lua (such as contour calculations) and expl3 code.
If someone else can help to produce the necessary changes to the repository, the author
will be grateful, but until then the over-complication of the package will
probably persist.

\section{Installation}

Place \texttt{wrapgraphics.sty}, \texttt{wrapgraphics.lua}, and
\texttt{wrapgraphics.py} in the same directory as your document
(or in \texttt{\$TEXMF/tex/latex/wrapgraphics/} for system-wide install).

\begin{itemize}
  \item Lua\LaTeX{} (with \texttt{--shell-escape})
  \item Python~3 + Pillow
\end{itemize}

\section{Package options}

A document that uses the \texttt{wrapgraphics} package should contain a line like the
following near the top of the document:

\begin{minted}{latex}
\usepackage[]{wrapgraphics}
\end{minted}

Supported options are \texttt{verbose} (\ref{package:verbose}) and \texttt{contour} (\ref{package:contour}).

\subsection{\texttt{verbose}}\label{package:verbose}

When enabled, additional diagnostic messages are written to the
terminal and log file.  It only exist for for debugging or performance
analysis.  Therefore, it should be disabled when compiling large documents.

\subsection{\texttt{contour}}\label{package:contour}

The \texttt{contour} option draws the traced contour over the
image for debugging and visualisation purposes.

This option can be enabled for each image separately.
The default contour colour is red; other colours can be specified.
All \texttt{xcolor} named colours and any colour defined via
\texttt{\string\definecolor} are supported.

\exampletable{Contour in red (default)}{contour-red}{contour-red.pdf}[
  The contour is drawn in red when no colour is given.]

\exampletable{Contour in green}{contour-green}{contour-green.pdf}[
  Green contour via \texttt{contour=green}.]

\exampletable{Custom purple contour}{contour-purple}{contour-purple.pdf}[
  Purple contour via \texttt{contour=customPurple}.]

\section{Macro options}

The \mintinline{latex}|\wrapgraphics| command accepts the options listed below,
passed as a comma-separated key--value list.

\subsection{\texttt{padding} (default: \texttt{5})}\label{macro:padding}

Extra clearance in pixels between the image edge and the wrapped text.
It is implemented by dilating the alpha mask before contour tracing.

Note that the value of this option is in pixels of the image.  Because of
it, the same value will produce different results for small and large images.

\exampletable{Padding 0\,px}{padding-00}{params-padding-00.pdf}[
  No padding: text follows the exact alpha contour.]

\exampletable{Padding 100\,px}{padding-50}{params-padding-50.pdf}[
  Very large padding pushes text far from the image.]

\subsection{\texttt{position} (default: \texttt{left})}\label{macro:position}

Which side of the image the text wraps around.  Accepted values are
\texttt{left}, \texttt{right}, and \texttt{middle}.
With \texttt{middle} the image is centred and text flows around it.

\exampletable{Position \texttt{left} (default)}{left}{basic.pdf}[
  Text wraps around the left side of the image.]

\exampletable{Position \texttt{right}}{right}{right.pdf}[
  Text wraps around the right side of the image.]

\subsection{\texttt{threshold} (default: \texttt{0.5})}\label{macro:threshold}

Alpha threshold (0--1).  Pixels with alpha greater than the threshold
are considered opaque; the contour runs along the boundary of opaque
pixels.  A low value treats more pixels as opaque, keeping the contour
closer to the visible image edge.  A high value lets text come closer
to the core of the image.

\exampletable{Threshold 0.3}{threshold-03}{params-threshold-03.pdf}[
  Low threshold: contour is close to the visible edge.]

\exampletable{Threshold 0.8}{threshold-08}{params-threshold-08.pdf}[
  High threshold: text can come closer to the core.]


\subsection{\texttt{scale} (default: \texttt{1})}\label{macro:scale}

Scale factor passed to \mintinline{latex}|\includegraphics|.  Overridden
by \texttt{width} if both are specified.

\exampletable{Scale 0.32}{left}{basic.pdf}[
  Image scaled to 32\,\% of its natural size.]


\subsection{\texttt{width}}\label{macro:width}

Explicit width passed to \mintinline{latex}|\includegraphics|,
e.g.\ \texttt{width=8cm}.  Overrides \texttt{scale}.

\exampletable{Width 8\,cm}{width}{width.pdf}[
  Image width set to 8\,cm.]


\subsection{\texttt{hang} (default: \texttt{false})}\label{macro:hang}

When set to \texttt{true} the indent becomes constant for all lines,
equal to the widest contour point.  Text is pushed by a fixed amount
rather than following the exact shape.

\exampletable{Hang left}{hang-left}{comparison-left-hang.pdf}[
  Left wrapping with a fixed indent equal to the widest point.]

\exampletable{Hang right}{hang-right}{comparison-right-hang.pdf}[
  Right wrapping with a fixed indent.]

\subsection{\texttt{lines}}\label{macro:lines}

Limit wrapping to the first $N$ lines; subsequent lines use the full
text width.

\exampletable{\texttt{lines=10}}{lines}{lines.pdf}[
  Wrapping is limited to 10 lines; text below uses full width.]

\exampletable{\texttt{lines=-12, skip=14}}{params-lines-skip}{params-lines-skip.pdf}[
  Negative \texttt{lines} reduces total wrapped lines from computed
  value; \texttt{skip} leaves the first N lines without indent.
  Here: 88 wrapped lines\kern-1pt, 14 skipped\kern-1pt, 74 indented.]


\subsection{\texttt{shift\_x}, \texttt{shift\_y}}\label{macro:shift}

Horizontal and vertical offset of the image
(e.g.\ \texttt{shift\_x=20pt}, \texttt{shift\_y=10pt}).
Positive \texttt{shift\_x} moves the image right;
positive \texttt{shift\_y} moves the image down (into the wrapping
paragraph); a negative \texttt{shift\_y} moves it up, overlaying the
text of the preceding paragraph (the wrapping shape only affects
lines from the \texttt{\string\wrapgraphics} command onward).

\exampletable{Shift right 20\,pt, down 50\,pt}{shift}{shift.pdf}[
  Positive shift moves the image right and down.
  Now the first line is always indented, just like with \texttt{shift\_y>0}.]

\exampletable{Negative shift}{shift-negative}{shift-negative.pdf}[
  Shift left by 40\,pt and up by 30\,pt.]
% TODO: А на этом примере почему-то не переместился вырез под картинку - а они должны перемещаться вместе.

\exampletable{Shift with right wrapping}{shift-combo}{shift-combo.pdf}[
  Right wrapping with \texttt{shift\_x=15pt, shift\_y=5pt}.]


\subsection{\texttt{anchor} (default: \texttt{here})}\label{macro:anchor}

Where to anchor the image.  The value \texttt{here} (default) places it
inline at the paragraph start.  The compass-point values
(\texttt{nw}, \texttt{ne}, \texttt{sw}, \texttt{se}) pin it to the
corresponding corner of the text area (north-west = top-left, etc.).

% TODO: Все примеры с якорями сломаны полностью. 

\exampletable{Anchor \texttt{ne}}{anchor}{anchor.pdf}[
  Image anchored to the top-right corner.]

\exampletable{Anchor \texttt{se}}{anchor-se}{anchor-se.pdf}[
  Image anchored to the bottom-right corner.]

\exampletable{Anchor \texttt{nw} with shift}{anchor-shift}{anchor-shift.pdf}[
  North-west anchor combined with \texttt{shift\_x=30pt, shift\_y=20pt}.]


\section{Examples}

\subsection{Multiple images}

Multiple \texttt{\string\wrapgraphics} calls can be placed throughout the
document.  Each call independently traces the contour and generates the
appropriate \texttt{\string\parshape}.

\exampletable{Multiple images}{multiple}{multiple.pdf}[
  Two \texttt{\string\wrapgraphics} calls in one document (left then right).]

\subsection{Large image sticking out}

A large image (wider than \texttt{\string\textwidth}) makes the wrapping
indentation exceed the text width; the right side of the image protrudes
into the margin.

\exampletablepair{Large image}{stickout}{stickout.pdf}[
  Image wider than \texttt{\string\textwidth}; right side protrudes
  into the margin.  Page~2 shows wrapping continues after the image
  height is exceeded.]


\subsection{Two-column layout}

\texttt{\string\wrapgraphics} works in \texttt{twocolumn} mode.
The wrapping area is sized to the column width.

\exampletable{Two-column left}{twocolumn}{twocolumn.pdf}[
  Left wrapping in a two-column document.]


\subsection{Two-column wide}

With a large scale (or width) the image becomes wider than one column
and reaches into the second column.

% TODO: Сдвинь картинку чуть влево. Когда картинка шире столбца в какой-то строке, то в этой строке ничего не должно быть написано. 
\exampletable{Two-column wide}{twocolumn-wide}{twocolumn-wide.pdf}[
  Image spanning two columns in twocolumn mode.]


\subsection{Two-column right}

Right-side wrapping also works in \texttt{twocolumn} mode.

\exampletable{Two-column right}{twocolumn-right}{twocolumn-right.pdf}[
  Right wrapping in two columns with more text.]

\subsection{Two-column middle}

In \texttt{twocolumn} mode, \texttt{position=middle} centres the image within
its column.  The contour is traced on both sides, so text wraps around the
image from both the left and right within that column.

\exampletable{Two-column middle}{twocolumn-middle}{twocolumn-middle.pdf}[
  Middle wrapping in the second column of a two-column layout.]


\section{Source files}

The following pages reproduce the source code with inline
documentation.  The source files use the special comment markers
\texttt{--[doc]} / \texttt{--[/doc]} (\textsf{.lua}),
\texttt{\#[doc]} / \texttt{\#[/doc]} (\textsf{.py}), and
\texttt{\%[doc]} / \texttt{\%[/doc]} (\textsf{.sty}) to delimit
\LaTeX{} documentation blocks.  These blocks are extracted by
\texttt{scripts/doc-extract.py} during \texttt{make doc} and
typeset as a \LaTeX{} fragment (this very section).

\input{wrapgraphics-sources.tex}

\end{document}
